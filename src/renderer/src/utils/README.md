# Информация о директории `utils`

`utils` -> вспомогательные функциии для возможного использования в других подмодулях

Директория `base` хранит в себе только то, что взаимодействует с базовыми функциями Vue
